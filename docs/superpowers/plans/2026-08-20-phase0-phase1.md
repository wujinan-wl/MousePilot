# MousePilot Phase 0 + Phase 1 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成兩個高風險技術驗證（SendInput vs GetLastInputInfo、SetSystemCursor 恢復），並建立可 build、可執行、可 publish 的 WPF 專案骨架（MVVM + Settings + Dashboard 殼）。

**Architecture:** 專案 csproj 直接放 repo 根目錄（符合規格 §39 驗收路徑 `bin\Release\net8.0-windows\win-x64\publish\`）；測試在 `tests\MousePilot.Tests\`；spike 程式在 `spikes\`（拋棄式，不進正式專案）。核心邏輯放 Services、PInvoke 集中 Native、View 只做綁定。

**Tech Stack:** .NET 8、WPF、CommunityToolkit.Mvvm 8.x、xUnit。

**Spec:** `docs/spec/mousepilot-spec.md`（總體計畫：`docs/superpowers/plans/2026-08-20-mousepilot-master-plan.md`）

## Global Constraints

- 目標框架 `net8.0-windows`、RID `win-x64`、SelfContained、PublishSingleFile、PublishReadyToRun（規格 §31；若造成問題可調整但必須寫明原因）。
- 相依只允許：`CommunityToolkit.Mvvm`（主專案）、`xunit` 系列（測試專案）。
- 設定檔路徑：`%AppData%\MousePilot\settings.json`（camelCase JSON，欄位名稱依規格 §18）。
- 設定範圍：idleStartSeconds `5~86400`、movementIntervalSeconds `1~86400`、movementPixels `1~100`、cursorSize ∈ {16,24,32,48,64,96,128}。
- 預設值：120 / 30 / 3 / Random / returnToOriginalPosition=true / startMinimized=true / autoStartMonitoring=true / customCursorEnabled=false / cursorSize=32 / Ctrl+Alt+F9 / Ctrl+Alt+F10。
- 一般錯誤不得讓程式退出；settings.json 損毀 → 備份 + 預設值 + 非侵入式提示。
- Commit 訊息：繁體中文 + 前綴（feat/fix/docs/chore/test/refactor），結尾附 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。不 push、不建 tag。
- Phase 0 的 spike 結論必須來自實際執行輸出，不得臆測。

---

### Task 1: Git 初始化與文件基礎 ✅（2026-08-20 已完成：git init -b main、remote origin=github.com/wujinan-wl/MousePilot、.gitignore、VERSION、CHANGELOG.md、初始 commit e162f91；另建立 .github/workflows/release.yml 與 .idea/vcs.xml）

**Files:**
- Create: `.gitignore`
- Create: `VERSION`
- Create: `CHANGELOG.md`

**Interfaces:**
- Produces: git repo（後續所有 Task 的 commit 依賴此）；`VERSION` 為唯一版本來源。

- [ ] **Step 1: git init 並建立 .gitignore**

```powershell
git init
```

建立 `.gitignore`：

```gitignore
# Build
bin/
obj/
publish/

# IDE
.vs/
.idea/
*.user

# Python scaffold 殘留（待使用者確認刪除 main.py 後移除此區）
.venv/
__pycache__/

# 本機暫存
*.log
```

- [ ] **Step 2: 建立 VERSION 與 CHANGELOG.md**

`VERSION`：

```text
0.1.0
```

`CHANGELOG.md`：

```markdown
# Changelog

本專案遵循 [Semantic Versioning](https://semver.org/lang/zh-TW/)。版本號唯一來源為 `VERSION` 檔。

## [Unreleased]

### 新增
- 專案文件：CLAUDE.md、需求規格、總體計畫、Phase 0+1 細部計畫。
```

- [ ] **Step 3: Commit**

```powershell
git add .gitignore VERSION CHANGELOG.md CLAUDE.md CLAUDE-preferences.md docs/
git commit -m @'
docs: 建立專案文件基礎（CLAUDE.md、規格、計畫、CHANGELOG、VERSION）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

注意：`main.py` 為 PyCharm 殘留檔，**不要**加入 commit；等使用者確認後刪除。

---

### Task 2: Spike A — SendInput 是否重置 GetLastInputInfo

**Files:**
- Create: `spikes/IdleSpike/IdleSpike.csproj`
- Create: `spikes/IdleSpike/Program.cs`

**Interfaces:**
- Produces: 實測結論（Task 4 寫入 findings 文件；Phase 2/3 的自我輸入隔離設計依據）。

- [ ] **Step 1: 建立 console 專案**

```powershell
dotnet new console -o spikes/IdleSpike -n IdleSpike
```

- [ ] **Step 2: 寫入 Program.cs**

```csharp
using System.Runtime.InteropServices;

// Spike A：驗證 SendInput 模擬滑鼠移動是否會重置 GetLastInputInfo。
// 執行方式：放開滑鼠鍵盤，等待程式在閒置滿 5 秒時送出一次相對移動 3px，
// 觀察後續印出的 idle 值是否歸零（歸零 = SendInput 會重置，正式版需要隔離機制）。
internal static class Program
{
    [StructLayout(LayoutKind.Sequential)]
    private struct LASTINPUTINFO { public uint cbSize; public uint dwTime; }

    [StructLayout(LayoutKind.Sequential)]
    private struct MOUSEINPUT
    {
        public int dx;
        public int dy;
        public uint mouseData;
        public uint dwFlags;
        public uint time;
        public IntPtr dwExtraInfo;
    }

    [StructLayout(LayoutKind.Sequential)]
    private struct INPUT
    {
        public uint type;
        public MOUSEINPUT mi;
    }

    private const uint INPUT_MOUSE = 0;
    private const uint MOUSEEVENTF_MOVE = 0x0001;

    [DllImport("user32.dll")]
    private static extern bool GetLastInputInfo(ref LASTINPUTINFO plii);

    [DllImport("user32.dll", SetLastError = true)]
    private static extern uint SendInput(uint nInputs, INPUT[] pInputs, int cbSize);

    private static uint IdleMs()
    {
        var info = new LASTINPUTINFO { cbSize = (uint)Marshal.SizeOf<LASTINPUTINFO>() };
        if (!GetLastInputInfo(ref info))
        {
            Console.WriteLine("GetLastInputInfo 失敗");
            return 0;
        }
        // TickCount 為 32-bit，49.7 天 wrap-around；用 unchecked 差值計算
        return unchecked((uint)Environment.TickCount - info.dwTime);
    }

    private static void Main()
    {
        Console.WriteLine("放開滑鼠鍵盤。閒置滿 5 秒時會送出一次 SendInput 相對移動 3px。");
        var sent = false;
        while (true)
        {
            var idle = IdleMs();
            Console.WriteLine($"idle = {idle} ms{(sent ? "（已送出模擬輸入）" : "")}");
            if (!sent && idle > 5000)
            {
                var inputs = new[]
                {
                    new INPUT
                    {
                        type = INPUT_MOUSE,
                        mi = new MOUSEINPUT { dx = 3, dy = 0, dwFlags = MOUSEEVENTF_MOVE },
                    },
                };
                var n = SendInput(1, inputs, Marshal.SizeOf<INPUT>());
                Console.WriteLine($"SendInput 送出，回傳 {n}（1=成功，0=失敗）");
                sent = true;
            }
            Thread.Sleep(500);
        }
    }
}
```

- [ ] **Step 3: Build 並執行**

```powershell
dotnet run --project spikes/IdleSpike
```

手動操作：放開滑鼠鍵盤 ≥ 6 秒，觀察並記錄：
1. SendInput 送出後下一筆 idle 值是否歸零（預期：會歸零 → 需要隔離機制）。
2. `SendInput` 回傳值是否為 1。

按 Ctrl+C 結束。把 console 實際輸出貼進 Task 4 的 findings 文件。

- [ ] **Step 4: Commit**

```powershell
git add spikes/IdleSpike
git commit -m @'
test: Spike A - 驗證 SendInput 對 GetLastInputInfo 的影響

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 3: Spike B — SetSystemCursor 替換與恢復

**Files:**
- Create: `spikes/CursorSpike/CursorSpike.csproj`
- Create: `spikes/CursorSpike/Program.cs`

**Interfaces:**
- Produces: 實測結論（Phase 9 全域游標設計依據）。

**安全原則：先驗證恢復路徑（`--restore-only`），再測試替換。**

- [ ] **Step 1: 建立 console 專案**

```powershell
dotnet new console -o spikes/CursorSpike -n CursorSpike
```

- [ ] **Step 2: 寫入 Program.cs**

```csharp
using System.Runtime.InteropServices;

// Spike B：驗證 SetSystemCursor 替換游標後，SPI_SETCURSORS 能否完整恢復。
// 用法：
//   dotnet run --project spikes/CursorSpike                  完整測試（替換 5 秒後恢復）
//   dotnet run --project spikes/CursorSpike -- --restore-only 只執行恢復（模擬 crash 後補救）
internal static class Program
{
    [DllImport("user32.dll")]
    private static extern IntPtr LoadCursor(IntPtr hInstance, int lpCursorName);

    [DllImport("user32.dll")]
    private static extern IntPtr CopyIcon(IntPtr hIcon);

    [DllImport("user32.dll", SetLastError = true)]
    private static extern bool SetSystemCursor(IntPtr hcur, uint id);

    [DllImport("user32.dll", SetLastError = true)]
    private static extern bool SystemParametersInfo(uint uiAction, uint uiParam, IntPtr pvParam, uint fWinIni);

    private const int IDC_CROSS = 32515;
    private const uint OCR_NORMAL = 32512;
    private const uint SPI_SETCURSORS = 0x0057;

    private static void Main(string[] args)
    {
        if (args.Length > 0 && args[0] == "--restore-only")
        {
            var ok = SystemParametersInfo(SPI_SETCURSORS, 0, IntPtr.Zero, 0);
            Console.WriteLine($"SPI_SETCURSORS 恢復：{ok}");
            return;
        }

        Console.WriteLine("按 Enter 後：標準箭頭 → 十字游標 5 秒 → SPI_SETCURSORS 恢復。");
        Console.ReadLine();

        // SetSystemCursor 會接管並銷毀傳入的 handle，必須傳複本（CopyIcon）
        var cross = CopyIcon(LoadCursor(IntPtr.Zero, IDC_CROSS));
        var setOk = SetSystemCursor(cross, OCR_NORMAL);
        Console.WriteLine($"SetSystemCursor：{setOk}");
        Thread.Sleep(5000);

        var restoreOk = SystemParametersInfo(SPI_SETCURSORS, 0, IntPtr.Zero, 0);
        Console.WriteLine($"SPI_SETCURSORS 恢復：{restoreOk}");
        Console.WriteLine("請目視確認箭頭游標已恢復原狀。");
    }
}
```

- [ ] **Step 3: 執行測試矩陣並記錄結果**

依序執行（每一項都目視確認游標狀態並記錄）：

1. **恢復路徑冒煙**（游標尚未被改動時執行，確認無副作用）：
   `dotnet run --project spikes/CursorSpike -- --restore-only`
2. **正常流程**：`dotnet run --project spikes/CursorSpike` → 確認替換成功、5 秒後自動恢復。
3. **模擬 crash**：再次執行，在十字游標顯示期間直接關閉 console 視窗（不讓它恢復）→ 確認游標停留在十字 → 執行 `--restore-only` → 確認恢復。
4. 若使用者目前用的是**非預設 cursor scheme**：確認 SPI_SETCURSORS 恢復的是使用者的 scheme 而不是 Windows 出廠預設。

- [ ] **Step 4: Commit**

```powershell
git add spikes/CursorSpike
git commit -m @'
test: Spike B - 驗證 SetSystemCursor 替換與 SPI_SETCURSORS 恢復

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 4: 記錄 Spike 結論

**Files:**
- Create: `docs/superpowers/research/2026-08-20-spike-findings.md`

**Interfaces:**
- Consumes: Task 2、Task 3 的實際執行輸出。
- Produces: Phase 2（自我輸入隔離）與 Phase 9（游標恢復）的設計依據文件。

- [ ] **Step 1: 依實測結果撰寫 findings 文件**

範本（「實測輸出」「結論」兩欄必須填入真實執行結果，不得照抄預期）：

```markdown
# Spike 結論：閒置偵測與全域游標（2026-08-20）

## Spike A：SendInput vs GetLastInputInfo

**問題（規格 §36A）：** MousePilot 自己的 SendInput 模擬滑鼠移動是否會重置 GetLastInputInfo？

**實測輸出：**（貼上 console 關鍵輸出）

**結論：**（會 / 不會 重置）

**Phase 2/3 設計決定：**
- 若會重置：MouseMovementService 在每次送出模擬輸入前，先呼叫
  IdleDetectionService 標記「抑制窗」（送出時刻 ± 容許誤差）。抑制窗內
  發生的 last-input 變化視為自我輸入、不重置 idle 計時；抑制窗外的任何
  輸入變化一律視為真實使用者操作（寧可誤判為使用者，也不可吃掉真實操作，
  符合規格 §40 原則 1）。
- 額外防線：送出前記錄預期游標座標，若偵測到的座標與預期不符 → 視為真實使用者。

## Spike B：SetSystemCursor 替換與恢復

**問題（規格 §36B）：** 自訂游標替換後能否可靠恢復使用者原本的 cursor scheme？

**測試矩陣結果：**

| 情境 | 結果 |
|------|------|
| 恢復路徑冒煙（未改動時執行 SPI_SETCURSORS） | |
| 正常流程（替換 → 5 秒 → 恢復） | |
| 模擬 crash（不恢復即結束 → --restore-only 補救） | |
| 非預設 cursor scheme 恢復 | |

**結論：**

**Phase 9 設計決定：**
- SetSystemCursor 必須傳 CopyIcon 複本（API 會銷毀傳入 handle）。
- 恢復一律用 SystemParametersInfo(SPI_SETCURSORS)（從使用者 registry scheme 重載，
  不需要自行保存/還原個別 cursor handle）。
- 恢復掛在：套用失敗、停用功能、程式結束、未處理例外 handler、SessionEnding。
- （若實測發現其他行為，補充於此）
```

- [ ] **Step 2: Commit**

```powershell
git add docs/superpowers/research/2026-08-20-spike-findings.md
git commit -m @'
docs: 記錄 Spike A/B 實測結論

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 5: WPF 專案骨架與發布設定

**Files:**
- Create: `MousePilot.csproj`、`App.xaml`、`App.xaml.cs`、`AssemblyInfo.cs`（由範本產生後修改）
- Create: `Views/MainWindow.xaml`、`Views/MainWindow.xaml.cs`（由範本 MainWindow 搬移）

**Interfaces:**
- Produces: 可 build 的 WPF 專案；`MousePilot` root namespace；`Views.MainWindow`（Task 10 填入 Dashboard 內容）。

- [ ] **Step 1: 產生 WPF 範本**

```powershell
dotnet new wpf -o . -n MousePilot
```

（目前根目錄沒有同名檔案，不會衝突。）

- [ ] **Step 2: 覆寫 MousePilot.csproj**

```xml
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <UseWPF>true</UseWPF>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>

    <SelfContained>true</SelfContained>
    <PublishSingleFile>true</PublishSingleFile>
    <PublishReadyToRun>true</PublishReadyToRun>

    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>MousePilot</RootNamespace>
    <AssemblyName>MousePilot</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.2.2" />
  </ItemGroup>

  <!-- 測試與 spike 專案在子目錄，避免被主專案誤編 -->
  <ItemGroup>
    <Compile Remove="tests\**;spikes\**" />
    <None Remove="tests\**;spikes\**" />
    <Page Remove="tests\**;spikes\**" />
  </ItemGroup>

</Project>
```

- [ ] **Step 3: 把 MainWindow 搬到 Views/**

1. 建立 `Views/` 資料夾，把範本產生的 `MainWindow.xaml`、`MainWindow.xaml.cs` 移入。
2. `MainWindow.xaml`：`x:Class` 改為 `MousePilot.Views.MainWindow`。
3. `MainWindow.xaml.cs`：namespace 改為 `MousePilot.Views`。
4. `App.xaml`：先保留 `StartupUri="Views/MainWindow.xaml"`（Task 10 會改為程式碼啟動）。

- [ ] **Step 4: Build 驗證**

```powershell
dotnet build -c Release
```

Expected: Build succeeded, 0 errors。

- [ ] **Step 5: Publish 冒煙（提早驗證規格 §31/§39，不等到 Phase 12）**

```powershell
dotnet publish -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true
```

Expected: `bin\Release\net8.0-windows\win-x64\publish\MousePilot.exe` 存在，雙擊可開出空視窗。

若 publish 失敗且原因在 csproj 屬性（例如 ReadyToRun 與環境衝突），依規格 §31 調整並在 commit 訊息與 CHANGELOG 寫明原因。

- [ ] **Step 6: Commit**

```powershell
git add MousePilot.csproj App.xaml App.xaml.cs AssemblyInfo.cs Views/
git commit -m @'
feat: 建立 WPF 專案骨架與單一 EXE 發布設定

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 6: xUnit 測試專案

**Files:**
- Create: `tests/MousePilot.Tests/MousePilot.Tests.csproj`

**Interfaces:**
- Consumes: `MousePilot.csproj`（ProjectReference）。
- Produces: 測試專案（Task 7/8 的測試放這裡）；測試指令 `dotnet test tests/MousePilot.Tests`。

- [ ] **Step 1: 建立測試專案並加入參考**

```powershell
dotnet new xunit -o tests/MousePilot.Tests -n MousePilot.Tests
dotnet add tests/MousePilot.Tests reference MousePilot.csproj
```

- [ ] **Step 2: 修改測試 csproj 與主專案相容**

`tests/MousePilot.Tests/MousePilot.Tests.csproj` 的第一個 `<PropertyGroup>` 改為：

```xml
  <PropertyGroup>
    <TargetFramework>net8.0-windows</TargetFramework>
    <UseWPF>true</UseWPF>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
```

（`net8.0-windows` + `UseWPF` + RID 對齊主專案，避免 ProjectReference 的 NETSDK 相容錯誤。其餘範本內容保留。）

- [ ] **Step 3: 刪除範本 UnitTest1.cs、跑一次測試確認管線通**

```powershell
Remove-Item tests/MousePilot.Tests/UnitTest1.cs
dotnet test tests/MousePilot.Tests
```

Expected: 0 個測試、exit code 0（或「找不到測試」但 build 成功）。若 `dotnet test` 因主專案 `SelfContained` 出現 NETSDK1201 等錯誤：把 `SelfContained/PublishSingleFile/PublishReadyToRun` 三行從主 csproj 移除、改為只靠 publish 命令列參數提供，並在 CHANGELOG 記錄原因（規格 §31 允許）。

- [ ] **Step 4: Commit**

```powershell
git add tests/
git commit -m @'
test: 建立 xUnit 測試專案

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 7: AppSettings 模型（TDD）

**Files:**
- Create: `Models/AppSettings.cs`
- Test: `tests/MousePilot.Tests/AppSettingsTests.cs`

**Interfaces:**
- Produces:
  - `enum MousePilot.Models.MovementMode { Horizontal, Vertical, Random }`
  - `class MousePilot.Models.AppSettings`：屬性見下方程式碼（int/bool/string），方法 `void Clamp()`，常數 `static readonly int[] AllowedCursorSizes`。
  - Task 8（SettingsService）、Task 9（MainViewModel）都以此為準。

- [ ] **Step 1: 寫失敗測試**

`tests/MousePilot.Tests/AppSettingsTests.cs`：

```csharp
using MousePilot.Models;

namespace MousePilot.Tests;

public class AppSettingsTests
{
    [Fact]
    public void 預設值符合規格()
    {
        var s = new AppSettings();
        Assert.Equal(120, s.IdleStartSeconds);
        Assert.Equal(30, s.MovementIntervalSeconds);
        Assert.Equal(3, s.MovementPixels);
        Assert.Equal(MovementMode.Random, s.MovementMode);
        Assert.True(s.ReturnToOriginalPosition);
        Assert.False(s.RunAtStartup);
        Assert.True(s.StartMinimized);
        Assert.True(s.AutoStartMonitoring);
        Assert.True(s.MinimizeToTrayOnClose);
        Assert.False(s.CustomCursorEnabled);
        Assert.Equal("", s.CursorFile);
        Assert.Equal(32, s.CursorSize);
        Assert.Equal(0, s.CursorHotspotX);
        Assert.Equal(0, s.CursorHotspotY);
        Assert.Equal("Ctrl+Alt+F9", s.ToggleHotkey);
        Assert.Equal("Ctrl+Alt+F10", s.RestoreCursorHotkey);
    }

    [Theory]
    [InlineData(0, 5)]
    [InlineData(4, 5)]
    [InlineData(5, 5)]
    [InlineData(120, 120)]
    [InlineData(86400, 86400)]
    [InlineData(100000, 86400)]
    public void IdleStartSeconds夾制在5到86400(int input, int expected)
    {
        var s = new AppSettings { IdleStartSeconds = input };
        s.Clamp();
        Assert.Equal(expected, s.IdleStartSeconds);
    }

    [Theory]
    [InlineData(0, 1)]
    [InlineData(1, 1)]
    [InlineData(30, 30)]
    [InlineData(100000, 86400)]
    public void MovementIntervalSeconds夾制在1到86400(int input, int expected)
    {
        var s = new AppSettings { MovementIntervalSeconds = input };
        s.Clamp();
        Assert.Equal(expected, s.MovementIntervalSeconds);
    }

    [Theory]
    [InlineData(0, 1)]
    [InlineData(3, 3)]
    [InlineData(100, 100)]
    [InlineData(500, 100)]
    public void MovementPixels夾制在1到100(int input, int expected)
    {
        var s = new AppSettings { MovementPixels = input };
        s.Clamp();
        Assert.Equal(expected, s.MovementPixels);
    }

    [Theory]
    [InlineData(16, 16)]
    [InlineData(48, 48)]
    [InlineData(33, 32)]   // 非允許尺寸 → 回到預設 32
    [InlineData(-1, 32)]
    public void CursorSize只允許規格清單(int input, int expected)
    {
        var s = new AppSettings { CursorSize = input };
        s.Clamp();
        Assert.Equal(expected, s.CursorSize);
    }
}
```

- [ ] **Step 2: 執行測試，確認失敗**

```powershell
dotnet test tests/MousePilot.Tests
```

Expected: 編譯失敗，錯誤為 `AppSettings`/`MovementMode` 不存在。

- [ ] **Step 3: 實作 Models/AppSettings.cs**

```csharp
namespace MousePilot.Models;

public enum MovementMode
{
    Horizontal,
    Vertical,
    Random,
}

/// <summary>
/// 應用程式設定。JSON 欄位名稱（camelCase）與範圍依規格 §18。
/// </summary>
public class AppSettings
{
    public static readonly int[] AllowedCursorSizes = { 16, 24, 32, 48, 64, 96, 128 };

    public int IdleStartSeconds { get; set; } = 120;
    public int MovementIntervalSeconds { get; set; } = 30;
    public int MovementPixels { get; set; } = 3;
    public MovementMode MovementMode { get; set; } = MovementMode.Random;
    public bool ReturnToOriginalPosition { get; set; } = true;
    public bool RunAtStartup { get; set; }
    public bool StartMinimized { get; set; } = true;
    public bool AutoStartMonitoring { get; set; } = true;
    public bool MinimizeToTrayOnClose { get; set; } = true;
    public bool CustomCursorEnabled { get; set; }
    public string CursorFile { get; set; } = "";
    public string CursorPreset { get; set; } = "";
    public int CursorSize { get; set; } = 32;
    public int CursorHotspotX { get; set; }
    public int CursorHotspotY { get; set; }
    public string ToggleHotkey { get; set; } = "Ctrl+Alt+F9";
    public string RestoreCursorHotkey { get; set; } = "Ctrl+Alt+F10";

    /// <summary>把所有數值夾制到規格允許範圍（載入與保存前呼叫）。</summary>
    public void Clamp()
    {
        IdleStartSeconds = Math.Clamp(IdleStartSeconds, 5, 86400);
        MovementIntervalSeconds = Math.Clamp(MovementIntervalSeconds, 1, 86400);
        MovementPixels = Math.Clamp(MovementPixels, 1, 100);
        if (!AllowedCursorSizes.Contains(CursorSize))
        {
            CursorSize = 32;
        }
    }
}
```

- [ ] **Step 4: 執行測試，確認通過**

```powershell
dotnet test tests/MousePilot.Tests
```

Expected: 全部 PASS。

- [ ] **Step 5: Commit**

```powershell
git add Models/AppSettings.cs tests/MousePilot.Tests/AppSettingsTests.cs
git commit -m @'
feat: AppSettings 模型與範圍夾制

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 8: SettingsService（TDD）

**Files:**
- Create: `Services/SettingsService.cs`
- Test: `tests/MousePilot.Tests/SettingsServiceTests.cs`

**Interfaces:**
- Consumes: `AppSettings`、`AppSettings.Clamp()`（Task 7）。
- Produces:
  - `class MousePilot.Services.SettingsService`：建構子 `SettingsService(string? settingsPath = null)`（null → `%AppData%\MousePilot\settings.json`）；`SettingsLoadResult Load()`；`void Save(AppSettings settings)`；`static string DefaultSettingsPath`。
  - `class MousePilot.Services.SettingsLoadResult`：`AppSettings Settings`、`bool WasCorrupt`、`string? BackupPath`。
  - Task 9（MainViewModel）使用 `Load()`/`Save()`。

- [ ] **Step 1: 寫失敗測試**

`tests/MousePilot.Tests/SettingsServiceTests.cs`：

```csharp
using MousePilot.Models;
using MousePilot.Services;

namespace MousePilot.Tests;

public sealed class SettingsServiceTests : IDisposable
{
    private readonly string _dir = Path.Combine(
        Path.GetTempPath(), "MousePilotTests", Guid.NewGuid().ToString("N"));

    private string SettingsPath => Path.Combine(_dir, "settings.json");

    public void Dispose()
    {
        try { Directory.Delete(_dir, recursive: true); } catch (IOException) { }
    }

    [Fact]
    public void 檔案不存在時回傳預設值且不標記損毀()
    {
        var result = new SettingsService(SettingsPath).Load();
        Assert.False(result.WasCorrupt);
        Assert.Equal(120, result.Settings.IdleStartSeconds);
    }

    [Fact]
    public void Save會自動建立資料夾()
    {
        new SettingsService(SettingsPath).Save(new AppSettings());
        Assert.True(File.Exists(SettingsPath));
    }

    [Fact]
    public void Save後Load保留設定值()
    {
        var service = new SettingsService(SettingsPath);
        service.Save(new AppSettings
        {
            IdleStartSeconds = 300,
            MovementMode = MovementMode.Vertical,
            ReturnToOriginalPosition = false,
        });

        var loaded = new SettingsService(SettingsPath).Load().Settings;
        Assert.Equal(300, loaded.IdleStartSeconds);
        Assert.Equal(MovementMode.Vertical, loaded.MovementMode);
        Assert.False(loaded.ReturnToOriginalPosition);
    }

    [Fact]
    public void JSON使用camelCase欄位名稱()
    {
        new SettingsService(SettingsPath).Save(new AppSettings());
        var json = File.ReadAllText(SettingsPath);
        Assert.Contains("\"idleStartSeconds\"", json);
        Assert.Contains("\"movementMode\"", json);
        Assert.Contains("\"Random\"", json); // enum 以字串保存
    }

    [Fact]
    public void 損毀JSON不擲例外且載入預設值()
    {
        Directory.CreateDirectory(_dir);
        File.WriteAllText(SettingsPath, "{ 這不是合法 JSON !!!");

        var result = new SettingsService(SettingsPath).Load();

        Assert.True(result.WasCorrupt);
        Assert.Equal(120, result.Settings.IdleStartSeconds);
    }

    [Fact]
    public void 損毀JSON會備份原始檔案()
    {
        Directory.CreateDirectory(_dir);
        File.WriteAllText(SettingsPath, "{ broken");

        var result = new SettingsService(SettingsPath).Load();

        Assert.NotNull(result.BackupPath);
        Assert.True(File.Exists(result.BackupPath));
        Assert.Equal("{ broken", File.ReadAllText(result.BackupPath!));
    }

    [Fact]
    public void 載入時超出範圍的值會被夾制()
    {
        Directory.CreateDirectory(_dir);
        File.WriteAllText(SettingsPath, "{\"idleStartSeconds\": 1, \"movementPixels\": 9999}");

        var loaded = new SettingsService(SettingsPath).Load().Settings;

        Assert.Equal(5, loaded.IdleStartSeconds);
        Assert.Equal(100, loaded.MovementPixels);
    }
}
```

- [ ] **Step 2: 執行測試，確認失敗**

```powershell
dotnet test tests/MousePilot.Tests
```

Expected: 編譯失敗，`SettingsService` 不存在。

- [ ] **Step 3: 實作 Services/SettingsService.cs**

```csharp
using System.IO;
using System.Text.Json;
using System.Text.Json.Serialization;
using MousePilot.Models;

namespace MousePilot.Services;

public sealed class SettingsLoadResult
{
    public required AppSettings Settings { get; init; }
    public bool WasCorrupt { get; init; }
    public string? BackupPath { get; init; }
}

/// <summary>
/// settings.json 載入/保存。損毀時：備份原檔 → 回傳預設值（不擲例外），符合規格 §18。
/// </summary>
public class SettingsService
{
    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        PropertyNamingPolicy = JsonNamingPolicy.CamelCase,
        PropertyNameCaseInsensitive = true,
        WriteIndented = true,
        Converters = { new JsonStringEnumConverter() },
    };

    public static string DefaultSettingsPath => Path.Combine(
        Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData),
        "MousePilot", "settings.json");

    private readonly string _settingsPath;

    public SettingsService(string? settingsPath = null)
    {
        _settingsPath = settingsPath ?? DefaultSettingsPath;
    }

    public SettingsLoadResult Load()
    {
        if (!File.Exists(_settingsPath))
        {
            return new SettingsLoadResult { Settings = new AppSettings() };
        }

        try
        {
            var json = File.ReadAllText(_settingsPath);
            var settings = JsonSerializer.Deserialize<AppSettings>(json, JsonOptions)
                           ?? throw new JsonException("settings 反序列化為 null");
            settings.Clamp();
            return new SettingsLoadResult { Settings = settings };
        }
        catch (Exception ex) when (ex is JsonException or IOException or UnauthorizedAccessException)
        {
            return new SettingsLoadResult
            {
                Settings = new AppSettings(),
                WasCorrupt = true,
                BackupPath = TryBackupCorruptFile(),
            };
        }
    }

    public void Save(AppSettings settings)
    {
        settings.Clamp();
        Directory.CreateDirectory(Path.GetDirectoryName(_settingsPath)!);
        File.WriteAllText(_settingsPath, JsonSerializer.Serialize(settings, JsonOptions));
    }

    private string? TryBackupCorruptFile()
    {
        try
        {
            var backupPath = _settingsPath + $".corrupt-{DateTime.Now:yyyyMMdd-HHmmss}.bak";
            File.Copy(_settingsPath, backupPath, overwrite: true);
            return backupPath;
        }
        catch (Exception ex) when (ex is IOException or UnauthorizedAccessException)
        {
            // 備份失敗不影響「載入預設值」主流程；Phase 11 接上 LogService 後記錄
            return null;
        }
    }
}
```

- [ ] **Step 4: 執行測試，確認通過**

```powershell
dotnet test tests/MousePilot.Tests
```

Expected: 全部 PASS。

- [ ] **Step 5: Commit**

```powershell
git add Services/SettingsService.cs tests/MousePilot.Tests/SettingsServiceTests.cs
git commit -m @'
feat: SettingsService - 載入/保存/損毀備份與預設值回復

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 9: MainViewModel

**Files:**
- Create: `ViewModels/MainViewModel.cs`
- Test: `tests/MousePilot.Tests/MainViewModelTests.cs`

**Interfaces:**
- Consumes: `SettingsService.Load()/Save()`（Task 8）、`AppSettings`（Task 7）。
- Produces:
  - `enum MousePilot.ViewModels.MonitorStatus { Paused, Monitoring, UserActive, WaitingToStart, AutoMoving }`
  - `class MousePilot.ViewModels.MainViewModel : ObservableObject`：`AppSettings Settings`、`MonitorStatus Status`、`string StatusText`、`double IdleSeconds`、`string MousePosition`、`string CursorStatusText`、`string Notice`、`void SaveSettings()`、`StartCommand`/`PauseCommand`（Phase 1 恆為 CanExecute=false，Phase 2 接上真實邏輯）。
  - Task 10（MainWindow）綁定以上屬性。

- [ ] **Step 1: 寫失敗測試**

`tests/MousePilot.Tests/MainViewModelTests.cs`：

```csharp
using MousePilot.Services;
using MousePilot.ViewModels;

namespace MousePilot.Tests;

public sealed class MainViewModelTests : IDisposable
{
    private readonly string _dir = Path.Combine(
        Path.GetTempPath(), "MousePilotTests", Guid.NewGuid().ToString("N"));

    private string SettingsPath => Path.Combine(_dir, "settings.json");

    public void Dispose()
    {
        try { Directory.Delete(_dir, recursive: true); } catch (IOException) { }
    }

    [Fact]
    public void 初始狀態為已暫停()
    {
        var vm = new MainViewModel(new SettingsService(SettingsPath));
        Assert.Equal(MonitorStatus.Paused, vm.Status);
        Assert.Equal("已暫停", vm.StatusText);
    }

    [Fact]
    public void 設定損毀時顯示非侵入式提示()
    {
        Directory.CreateDirectory(_dir);
        File.WriteAllText(SettingsPath, "{ broken");

        var vm = new MainViewModel(new SettingsService(SettingsPath));

        Assert.Contains("預設值", vm.Notice);
        Assert.Equal(120, vm.Settings.IdleStartSeconds); // 已回到預設，程式未 crash
    }

    [Fact]
    public void SaveSettings會保存並夾制範圍()
    {
        var vm = new MainViewModel(new SettingsService(SettingsPath));
        vm.Settings.IdleStartSeconds = 999999;

        vm.SaveSettings();

        var reloaded = new SettingsService(SettingsPath).Load().Settings;
        Assert.Equal(86400, reloaded.IdleStartSeconds);
    }

    [Fact]
    public void Phase1階段啟動與暫停命令停用()
    {
        var vm = new MainViewModel(new SettingsService(SettingsPath));
        Assert.False(vm.StartCommand.CanExecute(null));
        Assert.False(vm.PauseCommand.CanExecute(null));
    }
}
```

- [ ] **Step 2: 執行測試，確認失敗**

```powershell
dotnet test tests/MousePilot.Tests
```

Expected: 編譯失敗，`MainViewModel` 不存在。

- [ ] **Step 3: 實作 ViewModels/MainViewModel.cs**

```csharp
using CommunityToolkit.Mvvm.ComponentModel;
using CommunityToolkit.Mvvm.Input;
using MousePilot.Models;
using MousePilot.Services;

namespace MousePilot.ViewModels;

public enum MonitorStatus
{
    Paused,
    Monitoring,
    UserActive,
    WaitingToStart,
    AutoMoving,
}

public partial class MainViewModel : ObservableObject
{
    private readonly SettingsService _settingsService;

    public AppSettings Settings { get; }

    [ObservableProperty]
    private MonitorStatus _status = MonitorStatus.Paused;

    [ObservableProperty]
    private string _statusText = "已暫停";

    [ObservableProperty]
    private double _idleSeconds;

    [ObservableProperty]
    private string _mousePosition = "—";

    [ObservableProperty]
    private string _cursorStatusText = "Windows 預設";

    [ObservableProperty]
    private string _notice = "";

    public MainViewModel(SettingsService settingsService)
    {
        _settingsService = settingsService;
        var result = settingsService.Load();
        Settings = result.Settings;
        if (result.WasCorrupt)
        {
            Notice = result.BackupPath is null
                ? "設定檔損毀，已載入預設值。"
                : $"設定檔損毀，已載入預設值（原檔備份：{result.BackupPath}）。";
        }
    }

    public void SaveSettings() => _settingsService.Save(Settings);

    // Phase 2 接上 IdleDetectionService 後改為真實啟停邏輯與 CanExecute 條件
    [RelayCommand(CanExecute = nameof(CanStart))]
    private void Start() { }

    private bool CanStart() => false;

    [RelayCommand(CanExecute = nameof(CanPause))]
    private void Pause() { }

    private bool CanPause() => false;
}
```

- [ ] **Step 4: 執行測試，確認通過**

```powershell
dotnet test tests/MousePilot.Tests
```

Expected: 全部 PASS。

- [ ] **Step 5: Commit**

```powershell
git add ViewModels/MainViewModel.cs tests/MousePilot.Tests/MainViewModelTests.cs
git commit -m @'
feat: MainViewModel - 設定載入/保存與狀態屬性

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 10: Dashboard 殼與 App 接線

**Files:**
- Modify: `App.xaml`、`App.xaml.cs`
- Modify: `Views/MainWindow.xaml`、`Views/MainWindow.xaml.cs`

**Interfaces:**
- Consumes: `MainViewModel`（Task 9）全部屬性與命令。
- Produces: 可執行的 Dashboard 殼；App 啟動載入設定、視窗關閉保存設定。

- [ ] **Step 1: 覆寫 App.xaml（移除 StartupUri，加入卡片樣式）**

```xml
<Application x:Class="MousePilot.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>

        <!-- 狀態顏色（規格 §28） -->
        <SolidColorBrush x:Key="StatusRunningBrush" Color="#16A34A"/>
        <SolidColorBrush x:Key="StatusWaitingBrush" Color="#EAB308"/>
        <SolidColorBrush x:Key="StatusPausedBrush" Color="#9CA3AF"/>
        <SolidColorBrush x:Key="StatusErrorBrush" Color="#DC2626"/>

        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="CardTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>

    </Application.Resources>
</Application>
```

- [ ] **Step 2: 覆寫 App.xaml.cs**

```csharp
using System.Windows;
using MousePilot.Services;
using MousePilot.ViewModels;
using MousePilot.Views;

namespace MousePilot;

public partial class App : Application
{
    private MainViewModel? _mainViewModel;

    protected override void OnStartup(StartupEventArgs e)
    {
        base.OnStartup(e);

        _mainViewModel = new MainViewModel(new SettingsService());
        var window = new MainWindow { DataContext = _mainViewModel };
        MainWindow = window;
        window.Show();
    }

    protected override void OnExit(ExitEventArgs e)
    {
        _mainViewModel?.SaveSettings();
        base.OnExit(e);
    }
}
```

- [ ] **Step 3: 覆寫 Views/MainWindow.xaml**

```xml
<Window x:Class="MousePilot.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:MousePilot.Models"
        Title="MousePilot"
        Width="480" Height="640"
        MinWidth="420" MinHeight="560"
        Background="#F3F4F6">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16">

            <!-- 非侵入式提示（設定損毀等），Notice 為空時整塊隱藏 -->
            <Border>
                <Border.Style>
                    <Style TargetType="Border" BasedOn="{StaticResource Card}">
                        <Setter Property="Background" Value="#FEF9C3"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Notice}" Value="">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="{Binding Notice}" TextWrapping="Wrap"/>
            </Border>

            <!-- 狀態區 -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="狀態" Style="{StaticResource CardTitle}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Ellipse Width="10" Height="10" VerticalAlignment="Center"
                                 Fill="{StaticResource StatusPausedBrush}"/>
                        <TextBlock Text="{Binding StatusText}" Margin="8,0,0,0"
                                   FontSize="16" FontWeight="SemiBold"/>
                    </StackPanel>
                    <UniformGrid Columns="2" Rows="2">
                        <TextBlock>
                            <Run Text="閒置秒數："/><Run Text="{Binding IdleSeconds, Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock>
                            <Run Text="滑鼠座標："/><Run Text="{Binding MousePosition, Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock Text="距離觸發：—（Phase 2）"/>
                        <TextBlock>
                            <Run Text="游標："/><Run Text="{Binding CursorStatusText, Mode=OneWay}"/>
                        </TextBlock>
                    </UniformGrid>
                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <Button Content="啟動 MousePilot" Command="{Binding StartCommand}"
                                Padding="16,8" Margin="0,0,8,0"
                                ToolTip="Phase 2 完成閒置偵測後啟用"/>
                        <Button Content="暫停" Command="{Binding PauseCommand}" Padding="16,8"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- 自動移動設定 -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="自動移動設定" Style="{StaticResource CardTitle}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="34"/>
                            <RowDefinition Height="34"/>
                            <RowDefinition Height="34"/>
                            <RowDefinition Height="34"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="開始閒置（秒）" Style="{StaticResource FieldLabel}"/>
                        <TextBox Grid.Row="0" Grid.Column="1" VerticalAlignment="Center"
                                 Text="{Binding Settings.IdleStartSeconds}"/>

                        <TextBlock Grid.Row="1" Text="後續移動間隔（秒）" Style="{StaticResource FieldLabel}"/>
                        <TextBox Grid.Row="1" Grid.Column="1" VerticalAlignment="Center"
                                 Text="{Binding Settings.MovementIntervalSeconds}"/>

                        <TextBlock Grid.Row="2" Text="移動像素（px）" Style="{StaticResource FieldLabel}"/>
                        <TextBox Grid.Row="2" Grid.Column="1" VerticalAlignment="Center"
                                 Text="{Binding Settings.MovementPixels}"/>

                        <TextBlock Grid.Row="3" Text="移動模式" Style="{StaticResource FieldLabel}"/>
                        <ComboBox Grid.Row="3" Grid.Column="1" VerticalAlignment="Center"
                                  SelectedValuePath="Tag"
                                  SelectedValue="{Binding Settings.MovementMode}">
                            <ComboBoxItem Content="左右移動" Tag="{x:Static models:MovementMode.Horizontal}"/>
                            <ComboBoxItem Content="上下移動" Tag="{x:Static models:MovementMode.Vertical}"/>
                            <ComboBoxItem Content="隨機方向" Tag="{x:Static models:MovementMode.Random}"/>
                        </ComboBox>
                    </Grid>
                    <CheckBox Content="移動後回到原位置" Margin="0,8,0,0"
                              IsChecked="{Binding Settings.ReturnToOriginalPosition}"/>
                </StackPanel>
            </Border>

            <!-- Cursor 設定（Phase 7~9 實作，先佔位停用） -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="游標設定" Style="{StaticResource CardTitle}"/>
                    <TextBlock Text="自訂游標功能將在 Phase 7~9 提供。" Foreground="#6B7280"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Button Content="匯入圖片" IsEnabled="False" Padding="12,6" Margin="0,0,8,0"/>
                        <Button Content="套用" IsEnabled="False" Padding="12,6" Margin="0,0,8,0"/>
                        <Button Content="恢復 Windows 游標" IsEnabled="False" Padding="12,6"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- 系統設定 -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="系統設定" Style="{StaticResource CardTitle}"/>
                    <CheckBox Content="Windows 開機時自動啟動（Phase 5 生效）"
                              IsChecked="{Binding Settings.RunAtStartup}" Margin="0,0,0,6"/>
                    <CheckBox Content="啟動後最小化至系統匣（Phase 4 生效）"
                              IsChecked="{Binding Settings.StartMinimized}" Margin="0,0,0,6"/>
                    <CheckBox Content="啟動程式後自動開始監控（Phase 2 生效）"
                              IsChecked="{Binding Settings.AutoStartMonitoring}"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Window>
```

- [ ] **Step 4: 覆寫 Views/MainWindow.xaml.cs（唯一允許的 code-behind：關閉時保存設定）**

```csharp
using System.ComponentModel;
using System.Windows;
using MousePilot.ViewModels;

namespace MousePilot.Views;

public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
    }

    protected override void OnClosing(CancelEventArgs e)
    {
        (DataContext as MainViewModel)?.SaveSettings();
        base.OnClosing(e);
    }
}
```

- [ ] **Step 5: Build + 測試 + 手動驗證**

```powershell
dotnet build -c Release
dotnet test tests/MousePilot.Tests
dotnet run --project MousePilot.csproj
```

Expected：
1. Build/測試全綠。
2. 開出 Dashboard：狀態卡（已暫停、灰點、啟動/暫停按鈕停用）、自動移動設定卡（值為 120/30/3/隨機、checkbox 勾選）、游標卡（停用佔位）、系統設定卡。
3. 修改「開始閒置」為 300 → 關閉視窗 → 重開 → 值保留 300（確認 `%AppData%\MousePilot\settings.json` 生成）。
4. 手動把 settings.json 改成壞 JSON → 重開 → 程式不 crash、頂部顯示黃色提示、產生 `.corrupt-*.bak` 備份。

- [ ] **Step 6: Commit**

```powershell
git add App.xaml App.xaml.cs Views/
git commit -m @'
feat: Dashboard 卡片版面與設定載入/保存接線

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

### Task 11: Phase 收尾

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `docs/superpowers/plans/2026-08-20-mousepilot-master-plan.md`（進度總表）

**Interfaces:**
- Consumes: Task 1~10 全部完成。

- [ ] **Step 1: 更新 CHANGELOG.md 的 [Unreleased]**

在 `### 新增` 區塊補上：

```markdown
- Spike A/B：SendInput 對 GetLastInputInfo 影響、SetSystemCursor 恢復機制實測結論（docs/superpowers/research/）。
- WPF 專案骨架（.NET 8 / MVVM / CommunityToolkit.Mvvm），可 build 並 publish 為單一 EXE。
- AppSettings 模型與範圍夾制、SettingsService（settings.json 載入/保存/損毀備份回復）。
- Dashboard 卡片版面殼：狀態區、自動移動設定、游標佔位、系統設定。
- xUnit 測試專案與 Phase 1 單元測試。
```

- [ ] **Step 2: 更新總體計畫進度表**

把 Master Plan 進度總表中 Phase 0、Phase 1 的狀態改為 ✅ 完成。

- [ ] **Step 3: 最終驗證（全部指令一次跑綠）**

```powershell
dotnet build -c Release
dotnet test tests/MousePilot.Tests
dotnet publish -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true
```

Expected: 全部成功；`bin\Release\net8.0-windows\win-x64\publish\MousePilot.exe` 可雙擊執行。

- [ ] **Step 4: Commit**

```powershell
git add CHANGELOG.md docs/superpowers/plans/2026-08-20-mousepilot-master-plan.md
git commit -m @'
docs: 更新 CHANGELOG 與進度總表 - Phase 0/1 完成

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
'@
```

---

## Phase 0+1 完成定義

- [ ] Spike A/B 結論以實測輸出記錄於 findings 文件。
- [ ] `dotnet build -c Release` 成功、`dotnet test` 全綠。
- [ ] publish 產出單一 `MousePilot.exe` 且可執行。
- [ ] Dashboard 顯示四張卡片，設定修改後重開保留。
- [ ] settings.json 損毀 → 不 crash、備份、預設值、黃色提示（規格 §34 案例 29/30/31 的 Phase 1 版本）。
- [ ] 進度總表與 CHANGELOG 已更新。
